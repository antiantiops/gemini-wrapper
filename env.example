# Gemini API Key (OPTIONAL - not needed if using authenticated Gemini CLI)
# The application now uses host-based authentication through mounted .gemini folder
# Only set this if you want to override the authenticated session
# Get your API key from https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=your-api-key-here

# Application Port (optional, default: 8080)
PORT=8080
